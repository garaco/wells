
<nav class="navbar navbar-expand-lg bg-primary" id="nav-som">
  <!-- button Toggle -->
  {% if type == 'admin' %}
  <button type="button" class="btn btn-light navbar-toggle" id="menu-toggle">
    <i class="fa fa-bars" aria-hidden="true"></i>
  </button>
  {% endif %}

  <label class="icon-th-list" data-toggle="tooltip" title="Menú"></label>
  <a class="navbar-brand" href="{{ route('index') }}">
    <img src="{{ resource('img/logo.png') }}" width="40">
  </a>

  <ul class="navbar-nav flex-row ml-md-auto d-none d-md-flex">

    {% if type == 'cliente' or user == null %}
    <li class="nav-item">
      <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalLong" onclick="carrito();">
          <i class="fa fa-shopping-cart" aria-hidden="true" id="notifica">  </i>
      </button>
    </li>
    {% endif %}

    <li class="nav-item">
      <a class="nav-link text-white" href="{{ route('index') }}">Inicio</a>
    </li>

    {% if user %}
    <li class="nav-item dropdown">

        <button type="button" class="btn btn-light nav-item nav-link dropdown-toggle mr-md-2" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{user}}</button>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          {% if type == 'cliente' %}
            <a class="dropdown-item" href="{{ route('compra/user') }}"><i class="fa fa-shopping-bag"></i> Mis compras</a>
          {% endif %}
          <a class="dropdown-item" href="{{ route('logout') }}"><i class="fa fa-sign-out"></i> Cerrar sesión</a>
        </div>
      </li>
      {% else %}
      <li class="nav-item">
        <a class="nav-link text-white" href="{{ route('login') }}">Ingresar </a>
      </li>
    {% endif %}

  </ul>

</nav>
